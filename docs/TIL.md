## 2026-07-13

- CMake and its use, it manages the build process but does not compile code itself
- Ray tracing works by sending rays from a camera and calculating object intersections.
- A `Vec3` class can represent positions, directions, colors, and normals.
- `stb_image_write` is a single-header library used to save images like PNGs.